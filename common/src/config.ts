type CommonConfig = {
  stageSize: {
    width: number;
    height: number;
  };
  playerRadius: number;
};

export const COMMON_CONFIG: CommonConfig = {
  stageSize: {
    width: 576,
    height: 576,
  },
  playerRadius: 35,
};
